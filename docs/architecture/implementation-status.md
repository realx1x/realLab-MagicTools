# Implementation Status

This file records implementation evidence for the task plan. `Compiled` means the listed command completed on the stated host; it does not imply real-system lifecycle validation.

## Capability Matrix

| Capability | Windows x64 | macOS Intel | macOS Apple Silicon |
|---|---|---|---|
| Architecture decisions | Implemented | Implemented | Implemented |
| Job Object lifecycle spike | Compiled | Not applicable | Not applicable |
| Process group and libproc spike | Not applicable | Implemented; Runner pending | Implemented; Runner pending |
| Authenticated local IPC spike | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Supervisor ownership spike | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Cargo and pnpm workspace skeleton | Compiled | CI pending | CI pending |
| Tauri 2 and React shell | Compiled | CI pending | CI pending |
| Domain and generated TypeScript contracts | Compiled | Platform neutral | Platform neutral |
| Design tokens and UI primitives | Compiled | Platform neutral | Platform neutral |
| CI compile matrix | Configured | Configured | Configured |
| Supervisor single instance and runtime permissions | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Authenticated Windows Named Pipe transport | Compiled | Not applicable | Not applicable |
| Authenticated macOS Unix Socket transport | Not applicable | Implemented; Runner pending | Implemented; Runner pending |
| Tauri Bridge authenticated RPC and event forwarding | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| SQLite V1 migrations and Supervisor repository | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Revisioned snapshots, deltas, and UI resynchronization | Compiled | Platform neutral | Platform neutral |
| Bounded discovery scheduling and cache coordination | Compiled | Platform neutral | Platform neutral |
| Windows process snapshot and instance identity | Compiled | Not applicable | Not applicable |
| macOS process snapshot and instance identity | Not applicable | Implemented; Runner pending | Implemented; Runner pending |
| Windows TCP/UDP port ownership | Compiled | Not applicable | Not applicable |
| macOS TCP/UDP port ownership | Not applicable | Implemented; Runner pending | Implemented; Runner pending |
| Versioned process classification | Compiled | Platform neutral | Platform neutral |
| Project root normalization and evidence association | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Typed launch profile CRUD and validation | Compiled | Platform neutral | Platform neutral |
| Layered environment resolution and execution preview | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| System credential store and durable cleanup outbox | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Managed process launch | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Idempotent managed process stop state machine | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Authenticated external single-process stop | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Supervisor restart recovery reconciliation | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Bounded ordinary managed-run log capture | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Revisioned managed-log events and range backfill | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Streaming managed-log decoding and terminal-control filtering | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Optional interactive PTY/ConPTY managed sessions | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Managed-log secret redaction and bounded retention | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Application shell, theme, navigation, and responsive frame | Compiled | CI pending | CI pending |
| Virtualized process table, filtering, search, and sorting | Compiled | Platform neutral | Platform neutral |
| Process detail inspector and exact observed process tree | Compiled | Platform neutral | Platform neutral |
| Launch profile editor, secret-safe CRUD, and final execution preview | Compiled | CI pending | CI pending |
| Authoritative process stop controls and exact external stop confirmation | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Bounded managed-log history, live stream, and plain-text workbench panel | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Project and rule catalogs, redacted history, and truthful settings | Compiled | Platform neutral | Platform neutral |
| Tray lifecycle and authoritative managed-exit flow | Compiled | CI and Runner pending | CI and Runner pending |
| Keyboard-accessible, high-contrast, and responsive desktop UI | Compiled | CI and assistive-technology Runner pending | CI and assistive-technology Runner pending |
| Bounded discovery enrichment, event coalescing, and frozen snapshot delivery | Compiled; Runner measurement pending | Compiled; Runner measurement pending | Compiled; Runner measurement pending |
| Bounded structured diagnostics and redacted private export | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Hardened Tauri, authenticated IPC, and private database capability | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Crash-safe SQLite migration backup and recovery | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Explicitly authorized bounded test fixtures | Compiled | Implemented; Runner pending | Implemented; Runner pending |
| Pinned dependency audits, closed license policy, and target-aware CycloneDX SBOM | Configured; scheduled Runner pending | Configured; scheduled Runner pending | Configured; scheduled Runner pending |

## Execution Record

| Date | Task ID | Summary | Commands | Result |
|---|---|---|---|---|
| 2026-07-14 | P0-T01 | Added the ADR template, platform-spike template, system boundary map, and ADR-0001 through ADR-0005 for ownership, identity, IPC, stop semantics, and permission scope. | `git diff --check`; strict UTF-8 decode of all documentation files | Passed |
| 2026-07-14 | P0-T02 | Added the Windows Job Object lifecycle spike with suspended launch, fail-closed Job assignment, conditional graceful stop, force stop, breakaway semantics, and handle-based cleanup. | `cargo fmt --manifest-path experiments/windows-job-object/Cargo.toml -- --check`; Windows x64 `cargo check --manifest-path experiments/windows-job-object/Cargo.toml` using the pinned dependency from the local Cargo cache | Compiled; real-system validation not run |
| 2026-07-14 | P0-T03 | Added the macOS PGID lifecycle and bounded libproc process/FD/socket discovery spike with identity revalidation and access-limited errors. | `cargo fmt --manifest-path experiments/macos-process-group/Cargo.toml -- --check`; `cargo metadata --manifest-path experiments/macos-process-group/Cargo.toml --no-deps`; `rustup target list --installed` | Static checks passed; macOS Intel/Apple Silicon Runner compilation blocked because this host only has `x86_64-pc-windows-msvc` |
| 2026-07-14 | P0-T04 | Added current-SID Named Pipe ACL creation, private macOS Unix Socket setup, nonce-based mutual token proof, protocol negotiation, and bounded length-prefixed JSON frames. | `cargo fmt --manifest-path experiments/local-ipc-security/Cargo.toml -- --check`; Windows x64 `cargo check --manifest-path experiments/local-ipc-security/Cargo.toml` using the pinned dependency from the local Cargo cache | Windows compiled; macOS Runner and real cross-user validation pending |
| 2026-07-14 | P0-T05 | Added the Supervisor ownership actor: only the Supervisor holds child handles, run state, and disk log tasks; UI sessions have bounded channels only; explicit exit and upgrade handoff are separate operations. | `cargo fmt --manifest-path experiments/supervisor-ownership/Cargo.toml -- --check`; `cargo check --manifest-path experiments/supervisor-ownership/Cargo.toml --offline` | Windows compiled; no process, service, or test executed |
| 2026-07-14 | P0 gate | Rechecked all four Spike crates, strict UTF-8 decoding, manifests, and the no-recursive-PID fallback constraint. | All Spike `cargo fmt --check`; three Windows `cargo check`; macOS `cargo metadata`; `git diff --check` | Passed on Windows; macOS target compilation remains explicitly pending |
| 2026-07-14 | P1-T01 | Initialized the UTF-8 repository policy, pinned pnpm/Rust toolchains, lock files, root verification scripts, 11-crate Cargo workspace, and two TypeScript package skeletons. | `pnpm install --frozen-lockfile`; `pnpm format:check`; `pnpm typecheck`; `pnpm build:web`; `cargo fmt --all --check`; `cargo check --workspace --all-targets --offline`; `git diff --check` | Passed |
| 2026-07-14 | P1-T02 | Added the Tauri 2 desktop crate, React/Vite operational shell, strict local-only CSP, one minimum capability, and a reproducible development icon. | `pnpm format:check`; `pnpm typecheck`; `pnpm build:web`; `cargo fmt --all --check`; `cargo check -p desktop --all-targets`; `git diff --check` | Windows compiled; application not started |
| 2026-07-14 | P1-T03 | Added process identity/record, explicit field availability, port, classification, run state, and structured error contracts plus deterministic ts-rs exports. | `cargo run -p domain --bin export-types`; SHA-256 repeatability check; `cargo check -p domain --all-targets`; `pnpm typecheck`; `pnpm build:web`; `git diff --check` | Passed; generated output stable |
| 2026-07-14 | P1-T04 | Added exact light/dark tokens, Tailwind 4, Radix-based button/input/menu/tooltip/segmented controls, Lucide icons, focus states, and reduced-motion behavior. | `pnpm install --frozen-lockfile`; `pnpm format:check`; `pnpm typecheck`; `pnpm build:web`; `git diff --check` | Passed |
| 2026-07-14 | P1-T05 | Added a compile-only CI matrix for Windows x64, macOS Intel, and macOS Apple Silicon with pinned Node, pnpm, Rust, and fixed Action versions. | Prettier YAML parse through `pnpm format:check`; matrix target count check; `git diff --check` | Configuration passed; workflow not remotely executed |
| 2026-07-14 | P1 gate | Rechecked frozen dependencies, generated contracts, frontend formatting/types/build, Tauri and the full Rust workspace, strict UTF-8, and diffs. | Standard validation commands plus strict UTF-8 decode of 118 text files | Passed on Windows; macOS targets configured in CI and pending remote execution |
| 2026-07-14 | P2-T01 | Added OS-resolved current-user runtime paths, an exclusive Supervisor lock, and a redacted random session token. Windows validates handle type and owner, rejects reparse points, applies protected DACLs, and uses write-through atomic replace. macOS walks owner-validated no-follow directory handles, removes extended ACLs, applies 0700/0600 modes, and uses atomic rename. | `cargo fmt --all --check`; `cargo check -p supervisor --all-targets`; `git diff --check` | Windows compiled; macOS Runner pending; no Supervisor started |
| 2026-07-14 | P2-T02 | Added strict opaque wire messages, bounded length-prefixed framing, downgrade-resistant mutual token proof, consuming handshake phases, non-clone authenticated sessions, validated request/response/event/cancel envelopes, timeout limits, and closed method/event registries. | `cargo fmt --all --check`; `cargo check -p protocol --all-targets`; `cargo check -p domain --all-targets`; `git diff --check` | Passed; protocol is transport-neutral and no service or tests were run |
| 2026-07-14 | P2-T03 | Added a current-SID/current-session byte-mode Named Pipe endpoint, protected first-instance binding, remote-client rejection, client-session verification, a 16-instance bound, five-second/16 KiB pre-auth limits, typed authenticated client/server connections, and structured reconnect states with bounded backoff. | `cargo fmt --all --check`; `cargo check -p protocol --all-targets`; `cargo check -p supervisor --all-targets`; `git diff --check` | Windows compiled; no pipe, Supervisor, service, or test was started |
| 2026-07-14 | P2-T04 | Added an OS-account-derived macOS Unix Socket client and a fixed-root listener tied to the live single-instance guard. The adapter validates no-follow socket type/owner/mode and inode identity around cleanup, applies mode 0600, checks both peer effective UIDs, limits active connections to 16, enforces the shared five-second/16 KiB handshake boundary, and exposes bounded structured reconnect states. | `cargo fmt --all -- --check`; `cargo check -p protocol --all-targets`; `cargo check -p supervisor --all-targets`; `git diff --check` | Windows host checks passed; macOS Intel/Apple Silicon Runner compilation and audit-session peer validation remain pending; no socket, Supervisor, service, or test was started |
| 2026-07-14 | P2-T05 | Added the single-owner Tauri Bridge actor with a 64-command queue, 256 pending-request limit, bounded writes and replies, authenticated response correlation and event forwarding, structured connection state, fail-all disconnect behavior, non-replaying reconnects, secure per-platform token reads, and typed TypeScript invoke/listen wrappers. Setup only spawns the in-process actor and never launches a Supervisor or sidecar. | `cargo fmt --all -- --check`; `cargo check -p desktop --all-targets`; `pnpm format:check`; `pnpm typecheck`; `pnpm build:web`; `git diff --check` | Windows compiled and web checks passed; macOS Intel/Apple Silicon Runner compilation remains pending; no application, Supervisor, sidecar, service, or test was started |
| 2026-07-14 | P2-T06 | Added the seven-table V1 SQLite migration and a non-cloneable, single-connection Supervisor repository with transactional profile/environment and run/audit writes, immutable run identity binding, structured storage errors, explicit JSON text fields, absolute-path validation, and macOS database-file hardening. WAL, foreign keys, synchronous NORMAL, and a five-second busy timeout are configured before embedded transactional migrations run. | `cargo fmt --all -- --check`; `cargo check -p storage --all-targets`; `git diff --check`; strict UTF-8 decode of P2-T06 text files | Windows compiled; macOS file-mode code remains Runner pending; no database, migration, application, Supervisor, service, or test was opened or run |
| 2026-07-14 | P2-T07 | Added JavaScript-safe snapshot and process/port delta contracts, strict event revision validation, a Supervisor-owned in-memory revision state with deterministic snapshots and an explicit bounded lag signal, authenticated-connection generation forwarding, and a frontend snapshot store that subscribes before fetching, buffers during synchronization, rejects stale generations, replays only contiguous revisions, and invalidates on gaps, overflow, or disconnect. | Stable double generation of TypeScript contracts; `cargo fmt --all -- --check`; `cargo check -p domain -p protocol -p supervisor -p desktop --all-targets`; `pnpm format:check`; `pnpm typecheck`; `git diff --check` | Windows compiled and platform-neutral checks passed; no application, Supervisor, service, fixture, or test was started |
| 2026-07-14 | P2 gate | Rechecked the complete Rust workspace, production web build, strict UTF-8 decoding, authenticated revision flow, Bridge/UI responsibility boundaries, and absence of any wired real stop surface. | `cargo check --workspace --all-targets`; `pnpm build:web`; strict UTF-8 decode of 135 text files; static boundary and stop-surface scans; `git diff --check` | Passed on Windows; macOS implementation remains Runner pending where recorded; no application, Supervisor, service, fixture, real process, or test was started |
| 2026-07-14 | P3-T01 | Added the platform-neutral discovery backend boundary and bounded Tokio scheduler with two-second process and five-second port cadence, single-flight scan coalescing, cooperative cancel-and-restart, prioritized per-instance enrichment, bounded semantic caches, explicit availability states, sequenced delta subscriptions, lag-aware snapshot resynchronization, and convergent shutdown. | `cargo fmt --all -- --check`; `cargo check -p discovery --all-targets --locked`; `git diff --check`; strict UTF-8 decode of discovery text files | Compiled without warnings; no backend scan, native API, application, Supervisor, fixture, service, or test was run |
| 2026-07-14 | P3-T02 | Added the Windows x64 native process adapter using bounded `NtQuerySystemInformation` snapshots, an exact nonzero boot GUID, full-precision creation identity, checked x64 ABI parsing, minimal-rights handle revalidation, structured access-limited rows, canonical owner SIDs, image paths, Working Set memory, conservative thread status, two-sample total-capacity CPU metrics, and bounded command-line enrichment. | `cargo fmt --all -- --check`; `cargo check -p platform-windows -p discovery --all-targets --locked`; `git diff --check`; strict UTF-8 decode of platform-windows text files | Windows x64 compiled; no native query, process handle, real scan, application, Supervisor, service, fixture, or test was run; Windows 10/11 real-system validation remains pending |
| 2026-07-14 | P3-T03 | Added the 64-bit macOS process adapter using bounded `proc_listpids`, `KERN_BOOTTIME`, checked libproc ABI layouts, microsecond start identities with revalidation, structured TaskInfo access limits, resident memory, process state and UID, two-sample total-capacity CPU metrics, and bounded path, working-directory, and argv-only command-line enrichment. BSD-info-denied PIDs are skipped because a safe instance key cannot be constructed without native start time; no identity is fabricated. | `cargo fmt --all -- --check`; Windows-host `cargo check -p platform-macos -p discovery --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `git diff --check -- crates/platform-macos Cargo.lock`; strict UTF-8 decode of platform-macos text files | Host-side formatting, manifest, and empty-cfg checks passed; macOS Intel/Apple Silicon compilation remains Runner pending; no native query, real scan, application, Supervisor, service, fixture, or test was run |
| 2026-07-14 | P3-T04 | Added bounded Windows owner-PID table adapters for TCP/UDP over IPv4/IPv6 with checked ABI parsing, explicit complete-scan availability, IPv6 scope preservation, a one-million-row aggregate limit, deterministic endpoint/PID folding, field-level unknown TCP states, shared endpoint detection, and unknown-owner rows. Process attribution is emitted only when boot identity and full process creation time remain stable across snapshots bracketing all four tables. | `cargo fmt --all -- --check`; `cargo check -p platform-windows -p discovery --all-targets --locked`; staged, unstaged, and untracked-file whitespace checks; strict UTF-8 decode of platform-windows text files | Windows x64 compiled; no native port table, real scan, application, Supervisor, service, fixture, or test was run; Windows 10/11 shared-port and access behavior remain pending real-system validation |
| 2026-07-15 | P3-T05 | Added on-demand macOS per-instance TCP/UDP IPv4/IPv6 ownership through bounded `PROC_PIDLISTFDS` and `PROC_PIDFDSOCKETINFO` queries, checked 64-bit XNU ABI layouts, exact boot/PID/start identity revalidation, structured access states, field-level unknown TCP states, IPv6 scope preservation, descriptor-race tolerance, cooperative cancellation, deterministic folding, and a platform-enforced four-scan concurrency limit. Targeted results are merged into the scheduler cache without changing the global port snapshot from `Unknown`; owner removal and TTL expiry rebalance shared confidence and publish matching process/port deltas. | `cargo fmt --all -- --check`; Windows-host `cargo check -p discovery -p platform-windows -p platform-macos --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; staged and unstaged whitespace checks; strict UTF-8 decode of 148 text files | Host-side formatting, manifests, Windows compilation, and empty-cfg checks passed; macOS Intel/Apple Silicon compilation remains Runner pending; no native query, real scan, application, Supervisor, service, fixture, or test was run |
| 2026-07-15 | P3-T06 | Added a platform-neutral version-1 classification engine with the fixed plan scores, deterministic reasons, an explicit runtime-only gate, structured Include/Exclude/AssignProject rules, fail-closed Exclude precedence, priority-and-ID tie breaking, and bounded rule evaluation. The discovery actor atomically replaces validated rule snapshots, keeps project facts separate from classification decisions, and reclassifies on relevant fast-scan, enrichment, port, fact, and rule changes. Migration `0002` rebuilds classification rules with closed types/actions and a structured project foreign key without guessing incompatible legacy data. | `cargo fmt --all -- --check`; `cargo check -p discovery -p storage --all-targets --locked`; staged and unstaged whitespace checks; strict UTF-8 decode of discovery, storage, and migration files | Compiled without warnings; migration was embedded but no database was opened and no migration, application, Supervisor, fixture, service, or test was run |
| 2026-07-15 | P3-T07 | Added bounded project-root normalization and marker discovery with a versioned component-aware `mtpk1` key, fixed marker closed set, canonical Windows and macOS path handling, no-follow marker inspection, process working-directory revalidation, catalog association evidence, explicit Unknown/Missing/AccessLimited/NotSupported states, independent cancellable project scheduling, TTL/capacity-bounded caches, catalog-generation and instance-key stale-result rejection, and atomic evidence/classification replacement. Trusted project persistence now requires a non-serialized canonical-root/key pair and validates Windows invariant-Unicode casing plus complete canonical path components before SQL. | `cargo fmt --all -- --check`; `cargo check -p domain -p discovery -p storage -p platform-windows -p platform-macos --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; `git diff --check`; strict UTF-8 decode of 148 text files | Windows host formatting, manifests, platform-windows, discovery, domain, and storage compilation passed without warnings; macOS Intel/Apple Silicon target compilation and real canonical/reparse/marker behavior remain Runner pending; no native scan, database, migration, application, Supervisor, fixture, service, or test was run |
| 2026-07-15 | P3 gate | Rechecked that no stop interface was added before identity-safe lifecycle work, unavailable fields remain structured evidence rather than fabricated zero/empty values, and port ownership Unknown remains distinct from Known-empty bindings. Rechecked the full P3 Rust/frontend/UTF-8/whitespace gates after the trusted project-persistence hardening. | `cargo fmt --all -- --check`; `cargo check -p domain -p discovery -p storage -p platform-windows -p platform-macos --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; `git diff --check`; strict UTF-8 decode | Passed on Windows; macOS target and real-system validation remain explicitly pending in the capability matrix; no application, Supervisor, fixture, service, or test was started |
| 2026-07-15 | P4-T01 | Added closed Direct and explicit PowerShell/cmd/zsh Shell contracts with structured argv, a non-persistable Detected Script suggestion flow, server-owned profile identity/timestamps, create/update/delete DTOs with optimistic concurrency, bounded opaque-cursor listing, strict environment value variants, and generated TypeScript contract version 3. Lifecycle validation enforces bounded actual wire size, portable environment names, platform-safe lexical working directories, mode invariants, and stop-timeout limits. Storage now exposes only a typed domain facade over private row CRUD, canonicalizes argv JSON, uses four-item keyset pages, maps project/identity/name/version failures to structured errors, and rejects credential references until P4-T03 supplies a trusted system adapter. | Stable repeated `cargo run -p domain --bin export-types --locked` generation followed by Prettier; `cargo fmt --all -- --check`; `cargo check -p domain -p lifecycle -p protocol -p storage --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; `git diff --check`; strict UTF-8 decode of 166 text files | Node crates compiled without warnings and frontend contract checks passed. An additional combined desktop/Supervisor Cargo check timed out while waiting without compiler output and is not counted as successful evidence. No application, Supervisor, database, fixture, service, test, shell command, or real process was run. |
| 2026-07-15 | P4-T02 | Added a non-serializable Supervisor execution context, deterministic base/user/project/profile environment merging, case-correct PATH and PATHEXT parsing, redacted inherited and credential values, structured Direct/Shell invocation previews, and bounded lexical executable candidates. Windows cmd and PowerShell use absolute paths derived only from the trusted base `SystemRoot`; Direct rejects relative paths, implicit script execution, device namespaces, and Win32 trailing-dot/space aliases. macOS zsh is fixed to `/bin/zsh -f -c` and no login shell is consulted. Preview and native Windows UTF-16/macOS argument-environment budgets are bounded. Generated TypeScript contracts advanced to version 4. | Stable double `cargo run -p domain --bin export-types --locked` generation (raw tree SHA-256 `47FE0151869BF8301D82C17ABD66C941019220DD80FA1E3DAA3F4FE973056E12`) followed by Prettier; `cargo fmt --all -- --check`; `cargo check -p domain -p lifecycle --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; `git diff --check`; strict UTF-8 decode of 196 text files; targeted trailing-whitespace scan | Windows host compilation and frontend contract checks passed without warnings. Candidate status remains explicitly `FilesystemNotInspected`; the platform launch boundary must revalidate the selected executable before process creation. macOS Intel/Apple Silicon compilation remains Runner pending. No application, Supervisor, credential store, filesystem candidate scan, login shell, fixture, service, test, or real process was run. |
| 2026-07-15 | P4-T03 | Added canonical slot-bound `mtcred1` references, non-serializable zeroing secret buffers, write-only/redacted profile-secret requests, consuming Supervisor materialization, and credential-aware final environment resolution. Windows uses current-user Generic Credentials with bounded UTF-8 blobs, local-machine persistence, idempotent delete, RAII `CredFree`, and pre-free buffer clearing. macOS uses a fixed-service Generic Password adapter with bounded Security.framework FFI and RAII item/password cleanup. Migration `0003` adds a 256-entry bounded credential-cleanup outbox. New references are committed before the OS write, adopted with the profile, and removed/replaced references are enqueued atomically; the concrete per-platform `ProfileService` drains on startup and after successful or failed saves/deletes. WAL now uses `synchronous=FULL` so power loss cannot restore an old profile after its secret was deleted or lose a staged outbox row after a secret write. Generic protocol and Bridge request `Debug` output redacts all parameters. Generated TypeScript contracts advanced to version 5. | Stable double `cargo run -p domain --bin export-types --locked` generation (raw tree SHA-256 `B7F4031BBD52B58753F2AD3802DBB80DC876FAC98BE5866F5D33B240F312D1DB`) followed by Prettier; `cargo fmt --all -- --check`; `cargo check -p domain -p platform-common -p lifecycle -p storage -p platform-windows -p platform-macos -p protocol -p supervisor --all-targets --locked`; locked `cargo check -p desktop --all-targets` using the locally cached source mirror; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; `git diff --check`; strict UTF-8 decode of 203 text files | Windows credential, storage, Supervisor, protocol, lifecycle, domain, and desktop code compiled without warnings; frontend formatting and types passed. macOS adapter received host-side cfg/metadata checking, but Intel/Apple Silicon target compilation and real Keychain behavior remain Runner pending. The migration was embedded but no database was opened. No Credential Manager/Keychain operation, application, Supervisor, fixture, service, test, shell, or real process was run. |
| 2026-07-15 | P4-T04 | Added server-owned managed-run IDs, revision-pinned profile snapshots, bounded public start/result DTOs, and a typed `STARTING -> RUNNING/FAILED/ORPHANED` storage contract. The Windows adapter locks and identifies the exact executable file, creates it suspended with an explicit absolute application path and UTF-16 environment block, assigns a kill-on-close Job, reads identity from the same process handle, and resumes only after identity persistence. Job cleanup is confirmed only when Job `ActiveProcesses` reaches zero; unconfirmed cleanup retains retryable control handles. The Supervisor executes launch/profile mutations in detached serialized tasks so client cancellation cannot interrupt ownership transitions, retains handles across database awaits, retries pending cleanup, terminates the Job if Running persistence fails, and publishes the process only after durable Running state. Interactive profiles remain explicitly unsupported until P5. Generated TypeScript contracts advanced to version 6. | Stable double `cargo run -p domain --bin export-types --locked` generation (raw tree SHA-256 `1D270A6332F889E363EED3792AA61A732977711EF952D40FE701038752BF313B`) followed by Prettier; `cargo fmt --all -- --check`; `cargo check -p domain -p lifecycle -p storage -p platform-windows -p supervisor --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; `git diff --check`; strict UTF-8 decode of 209 text files | Windows domain, lifecycle, storage, native launch, and Supervisor orchestration compiled without warnings; generated frontend contracts, formatting, and types passed. No database was opened and no application, Supervisor, credential operation, fixture, service, test, shell, or real process was run. Windows 10/11 lifecycle behavior remains pending explicit real-system validation. |
| 2026-07-16 | P4-T05 | Added a macOS managed-launch adapter using exact-path `posix_spawn`, a dedicated suspended POSIX process group, explicit argv/environment buffers with zeroing cleanup, Supervisor-owned identity and PGID persistence before `SIGCONT`, and identity-plus-group revalidation immediately before resume. Migration `0004` persists the nullable macOS PGID while Windows Job runs remain `NULL`; native, lifecycle, storage, and SQL boundaries all enforce that the dedicated PGID equals the root PID. Cleanup sends bounded `SIGKILL`, reaps the owned root, confirms PGID absence, and retains retryable evidence when cleanup cannot be proved. Native launch validation failures are classified as process creation rather than identity reads. Generated TypeScript contracts advanced to version 7. | Stable double `cargo run -p domain --bin export-types --locked` generation (raw tree SHA-256 `44C84D3A642E048EBE5A20D9C8D1CFFBDFCCB7C2BAC7B3BBF99E72A5069F0AE9`) followed by Prettier; `cargo fmt --all -- --check`; `cargo check -p domain -p lifecycle -p storage -p platform-windows -p platform-macos -p supervisor --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; staged and unstaged `git diff --check`; strict UTF-8 decode of 217 text files | Windows-host compilation, manifests, generated contracts, formatting, types, UTF-8, and whitespace checks passed. This host has only `x86_64-pc-windows-msvc`; macOS Intel/Apple Silicon target compilation and native ABI/lifecycle validation remain Runner pending. No database, migration, credential operation, application, Supervisor, fixture, service, managed shell, or real development process was opened or run. A failed initial `ORPHANED` state write remains deferred to P4-T08 recovery reconciliation; an already persisted `ORPHANED` state intentionally remains conservative after a later cleanup retry. |
| 2026-07-16 | P4-T06 | Added distinct graceful and force managed-stop contracts, authenticated-envelope operation ID validation, generated TypeScript contract version 8, and migration `0005` with globally idempotent operations, one active operation per run, exact graceful-to-force supersession, and transactional operation/run CAS transitions. Windows revalidates boot/PID/creation identity before `CTRL_BREAK_EVENT` or Job termination; macOS revalidates boot/PID/start/PGID before `SIGTERM` or `SIGKILL` and distinguishes root identity changes from lost process-group control. The Supervisor reserves and persists one signal attempt, starts timeout accounting only after attempt evidence is durable, never auto-forces a timed-out graceful stop, blocks force supersession while signal or completion evidence is committing, and retains quarantined ownership unless complete exit is confirmed. Natural exit, unavailable signals, poll failures, identity mismatch, orphaning, and timeout remain closed durable outcomes; unconfirmed outcomes never fabricate `endedAt`. | Stable double `cargo run -p domain --bin export-types --locked` generation (raw tree SHA-256 `311F7684E1562652C551ED4A336AE4105F9D891385A22AFFB35801C9CA9B0B8A`) followed by Prettier; `cargo fmt --all -- --check`; `cargo check -p domain -p lifecycle -p protocol -p storage -p platform-windows -p platform-macos -p supervisor --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; staged and unstaged `git diff --check`; strict UTF-8 decode of 226 text files; `rustup target list --installed` | Windows-host compilation, manifests, generated contracts, formatting, types, UTF-8, and whitespace checks passed. This host has only `x86_64-pc-windows-msvc`; macOS Intel/Apple Silicon target compilation and native signal/ABI validation remain Runner pending. No database, migration, application, Supervisor, fixture, service, shell, test, signal, or real development process was opened or run. |
| 2026-07-16 | P4-T07 | Added an explicit confirmation contract for one exact external `ProcessInstanceKey`, a closed `SingleProcess` scope, signal-delivered-only results, and generated TypeScript contract version 9. The Supervisor derives the protected desktop PID from the same authenticated transport using `GetNamedPipeClientProcessId` or Darwin `LOCAL_PEERPID`; opaque peer identity, validated request envelope, and connection lifetime remain inseparably borrowed so concurrent connections cannot exchange protection identities. Windows pins one handle while revalidating boot/PID/creation time, current non-elevated user/session, and critical status before one `TerminateProcess`; macOS revalidates boot/PID/start time and real/effective/saved UID/GID before one positive-PID `SIGTERM`. PID 0/1/4 where applicable, the Supervisor, and the authenticated desktop peer are protected; neither platform enumerates descendants, retries, elevates, or substitutes a stronger stop. A 4096-entry Supervisor-incarnation ledger replays the exact result across authenticated reconnects, rejects operation-ID rebinding, and fails closed at capacity; transient external observations are not persisted across Supervisor restarts. | Stable double `cargo run -p domain --bin export-types --locked` generation (path-delimited raw tree SHA-256 `A973746A9BC7B316B5A44D89364CE125E5465C13F21262F847C7122DF5932746`) followed by Prettier; offline lockfile refresh for the direct Supervisor `serde` dependency; `cargo fmt --all -- --check`; `cargo check -p domain -p lifecycle -p protocol -p platform-windows -p platform-macos -p supervisor --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; staged and unstaged `git diff --check`; strict UTF-8 and trailing-whitespace scan | Windows-host compilation, manifests, generated contracts, formatting, types, UTF-8, and whitespace checks passed. macOS has no stable external-process handle, so the minimized inspection-to-`kill` race plus Intel/Apple Silicon compilation and native ABI/signal behavior remain Runner pending. No database, migration, application, Supervisor, fixture, service, shell, test, process handle, signal, or real development process was opened or run. |
| 2026-07-16 | P4-T08 | Added bounded ID-keyset startup reconciliation with transactionally compared run and active-stop snapshots, closed `Recovered`/`ExitedWhileOffline`/`IdentityMismatch`/`Orphaned` outcomes, and fail-closed validation for incomplete identity, missing control groups, terminal recovery markers, and ended runs. Windows pins a query-only process handle for identity and exit evidence but never recreates an anonymous Job control; an exact live instance is therefore `Orphaned`. macOS requires canonical boot/start identity, matching BSD and `getpgid` observations, and a persisted leader PGID before publishing an independent recovered process-group control. Recovered controls own no child, wait right, or log pipe, have signal-free drop, and revalidate the complete identity and group before each `killpg`. `STARTING` and `SIGNAL_PENDING` may collect exit or mismatch evidence but exact-live instances never regain control or resend a signal. Recovered `REQUESTED` operations continue their durable reservation flow, while `IN_PROGRESS` and `TIMED_OUT` only poll; explicit force supersession moves the same recovered control. `ManagedRunService::open/new` finishes every candidate page before returning, persists the CAS before publishing macOS control, and supports immutable profile snapshots after profile deletion. The TypeScript wire contract remains version 9. | `cargo fmt --all -- --check`; `cargo check -p domain -p lifecycle -p protocol -p storage -p platform-windows -p platform-macos -p supervisor --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; staged and unstaged `git diff --check`; strict UTF-8 and trailing-whitespace scan; `rustup target list --installed` | Windows-host compilation, storage/Supervisor/platform-neutral contracts, formatting, frontend types, UTF-8, and whitespace checks passed. This host has only `x86_64-pc-windows-msvc`; macOS Intel/Apple Silicon cfg compilation and native identity/PGID/signal behavior remain Runner pending. No database, migration, credential operation, application, Supervisor, fixture, service, shell, test, process probe, process handle, signal, or real development process was opened or run. |
| 2026-07-16 | P4 gate | Rechecked that managed graceful/force stop and confirmed external single-process stop remain distinct contracts, every incomplete cleanup or recovery boundary is explicitly orphaned, no adapter recursively stops external descendants, and neither launch, stop, nor recovery introduces elevation or permission fallback. Recovery persistence refuses incomplete identity, missing macOS PGID, ended live rows, terminal recovery markers, and ambiguous signal-pending control publication. | `cargo fmt --all -- --check`; `cargo check -p domain -p lifecycle -p protocol -p storage -p platform-windows -p platform-macos -p supervisor --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; staged and unstaged `git diff --check`; strict UTF-8 and trailing-whitespace scan of 235 text files; `rustup target list --installed` | Passed on Windows without warnings. macOS Intel/Apple Silicon compilation and native lifecycle validation remain explicitly pending; no application, Supervisor, database, fixture, service, test, process probe, process handle, signal, shell, or real development process was opened or run. |
| 2026-07-16 | P5-T01 | Added independent bounded stdout/stderr pipe capture for non-interactive managed runs, fixed 16 KiB pipe draining, 10 MiB rolling files with five files per stream, and 256 KiB in-memory tails per stream. Disk failure is latched while each pipe continues draining to EOF, so a slow or disconnected UI cannot block managed output. Windows opens a private current-user, local-disk run directory, rejects reparse points and hard-linked files, and launches with `STARTUPINFOEXW` plus a three-handle inheritance allowlist. macOS uses owner-checked no-follow directory traversal, private modes, cleared extended ACLs, `F_DUPFD_CLOEXEC`, explicit `dup2`/close actions, and `POSIX_SPAWN_CLOEXEC_DEFAULT`. Supervisor prepares logs before native creation, retains both capture workers with the launched control, and classifies every stdio preparation failure as `launch:logPreparation`; recovered processes intentionally expose no live log pipe. | `cargo fmt --all`; `cargo fmt --all -- --check`; `cargo check -p logging -p storage -p platform-windows -p platform-macos -p supervisor --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; staged and unstaged `git diff --check`; strict UTF-8 and trailing-whitespace scan of 231 text files | Windows-host compilation, formatting, manifests, UTF-8, whitespace, and static Windows/macOS pipe-lifetime reviews passed. macOS Intel/Apple Silicon cfg compilation and native fd behavior remain Runner pending; Windows real child-handle inheritance also remains unexecuted. No application, Supervisor, database, fixture, service, shell, test, child process, or real development process was opened or run. |
| 2026-07-16 | P5-T02 | Added capacity-one nonblocking dirty notifications, 50 ms stdout/stderr batching, independent stream sequences, 64 KiB chunk/range limits, and one shared global revision publication timeline for process, port, and log events. Slow or disconnected consumers can lag only bounded event queues while capture continues to disk. Range reads are limited to four concurrent requests, use database-owned run identity plus an exact Supervisor log-root check, preserve one offset generation through a 256-entry active/LRU reader registry, and return an explicit stale-generation conflict instead of silently remapping an evicted explicit offset. Event and range contracts expose retained windows, EOF, caught-up state, sanitized disk/read/delivery failures, and explicit unknown historical IO status. The TypeScript contract advanced to version 11 with strict typed `log.chunk` and `run.get_log_range` helpers; the snapshot store validates log batches and advances revision without retaining log bodies. | Stable double `cargo run -p domain --bin export-types --locked` generation (raw tree SHA-256 `791F12E4F9A6120DFBD0BA5D7AE7D5E545C675180B1F5CB579FFCD933005B354`) followed by Prettier; `cargo fmt --all -- --check`; `cargo check --workspace --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; staged and unstaged `git diff --check`; strict UTF-8 and trailing-whitespace scan of 251 text files; `rustup target list --installed` | Windows workspace, generated contracts, formatting, frontend types, metadata, UTF-8, and whitespace checks passed. This host has only `x86_64-pc-windows-msvc`; macOS Intel/Apple Silicon target compilation and native rolling-file behavior remain Runner pending. Thread-failure injection and real slow-client pressure validation were not run. No application, Supervisor, database, fixture, service, shell, test, child process, or real development process was opened or run. |
| 2026-07-16 | P5-T03 | Added streaming managed-log decoding before every storage and publication boundary. macOS uses UTF-8; Windows Direct uses the active ANSI code page while cmd and PowerShell use the active OEM code page. BOM and valid UTF-8 evidence take precedence, invalid or truncated input emits replacement text without stopping pipe drainage, and unsupported automatic fallback records an explicit status. The stateful terminal filter removes OSC 52, hyperlinks, CSI, DCS, APC, PM, SOS, and ESC sequences, normalizes CRLF and lone CR to LF, rejects unsafe Unicode control and separator categories, and preserves plain text such as `<script>` without HTML rendering. Rolling files, memory tails, events, range reads, offsets, public append, and historical files all preserve complete UTF-8 scalar boundaries. Generated TypeScript contracts advanced to version 12 with exact-key, status, scalar-safety, byte-length, and range-bound validation. | Stable double `cargo run -p domain --bin export-types --locked` generation (raw tree SHA-256 `4FF316A2FD422F9D3AB973E838C16F571FB5DBC1F068DD89BD059525AF3BDE33`) followed by Prettier; `cargo fmt --all -- --check`; `cargo check --workspace --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; staged and unstaged `git diff --check`; strict UTF-8 and trailing-whitespace scan of 249 text files; `rustup target list --installed` | Windows workspace, generated contracts, formatting, frontend types, metadata, UTF-8, and whitespace checks passed. This host has only `x86_64-pc-windows-msvc`; macOS Intel/Apple Silicon compilation and native code-page/pipe behavior remain Runner pending. Compile-only decoder, filter, and UTF-8 boundary test code was added but not run. No application, Supervisor, database, fixture, service, shell, test, child process, signal, or real development process was opened or run. |
| 2026-07-16 | P5-T04 | Added an explicit launch-I/O split so ordinary profiles retain their independent stdout/stderr pipes while only interactive profiles allocate a terminal. Windows uses native ConPTY with one continuously drained UTF-8 output pipe, Supervisor-owned input and resize capability, `CREATE_SUSPENDED`, pre-resume Job assignment and identity persistence, and Job emptiness as the only exit truth; ConPTY runs do not claim unsupported `CTRL_BREAK` process-group control. macOS uses `openpty`, close-on-exec descriptors, one slave duplicated to fd 0/1/2, one retained duplex master, Darwin `EIO`-as-EOF handling, and the existing suspended dedicated process-group identity boundary. PTY output is a filtered merged stdout transcript and stderr closes as an empty stream; no terminal EOF directly changes run state. A terminal-only natural-exit monitor confirms the native Job/process-group boundary before an idempotent `RUNNING -> EXITED` storage CAS and yields to the existing stop state machine on every retry. The public/generated wire contract remains version 12. | `cargo fmt --all -- --check`; `cargo check --workspace --all-targets --locked`; targeted `cargo check -p platform-macos -p platform-windows -p storage -p supervisor --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; staged and unstaged `git diff --check`; strict UTF-8 and trailing-whitespace scan of 249 text files; `rustup target list --installed` | Windows workspace, ConPTY adapter, storage CAS, Supervisor orchestration, formatting, frontend types, metadata, UTF-8, and whitespace checks passed without warnings. This host has only `x86_64-pc-windows-msvc`; macOS Intel/Apple Silicon cfg compilation and native `openpty`/fd/ABI behavior remain Runner pending. The macOS adapter intentionally preserves the existing safe `posix_spawn` boundary and does not claim a controlling terminal; full job-control behavior remains a Runner risk. Terminal wire input/resize and VT rendering remain a P6 presentation/IPC concern; platform owners already retain those capabilities. No application, Supervisor, database, fixture, service, shell, test, child process, signal, PTY, ConPTY, or real development process was opened or run. |
| 2026-07-16 | P5-T05 | Added one shared sensitive-field-name rule across lifecycle validation, audit details, and runtime logging. Sensitive environment names now require credential references; compatible inherited/legacy plain values are still included in the exact runtime rule set. After decode and terminal filtering, each stdout/stderr or merged PTY stream performs stateful leftmost-longest literal replacement across arbitrary chunks with a fixed `[REDACTED]` marker; rule, carry, decoder, and pipe scratch buffers receive best-effort clearing, and no public collector append/no-redaction capture shortcut remains. Migration `0006` records log-redaction provenance, deletion-started and deletion-completed markers; historical range reads reject legacy version-zero logs and any deletion-pending run. Retention keeps the existing 100 MiB per-run ceiling, reserves a 1 GiB admission budget across active and retained logs, limits active runs to 10 and retained terminal directories to 256, removes logs older than 30 days oldest-first, drains audit expiry in bounded batches, runs at startup/every six hours/before admission, and blocks new starts while backlog or capacity remains unresolved. Deletion requires a terminal database candidate, no control or active capture reader, all four range permits, a persisted intent, fixed-name handle-relative removal, and a final CAS. Cancellation cannot release permits while blocking deletion continues. Windows pins a no-delete-share root handle and uses handle disposition; macOS pins a root dirfd and uses `openat`/`unlinkat`. Arbitrary audit writes are crate-private and summaries/details accept only bounded closed-code shapes without sensitive/content fields. The public/generated wire contract remains version 12. | `cargo fmt --all`; `cargo fmt --all -- --check`; `cargo check -p platform-common -p lifecycle -p logging -p storage -p platform-windows -p platform-macos -p supervisor --all-targets --locked`; `cargo check --workspace --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; staged and unstaged `git diff --check`; strict UTF-8 and trailing-whitespace scan of 254 text files; `rustup target list --installed` | Windows workspace compilation, formatting, frontend types, metadata, migration embedding, UTF-8, whitespace, and independent redaction/retention reviews passed without warnings. Only `x86_64-pc-windows-msvc` is installed; macOS Intel/Apple Silicon compilation and native root-dirfd/file-removal behavior remain Runner pending. Migration `0006` was modified only before any execution and no database was opened. Diagnostics export and notification/history presentation remain their planned P6/P7 consumers; current public summaries contain no command, environment, or secret, and untrusted historical log bytes fail closed. No application, Supervisor, database, migration, credential store, fixture, service, test, shell, child process, signal, PTY, ConPTY, or real development process was opened or run. |
| 2026-07-16 | P5 gate | Rechecked that capture remains independently bounded and nonblocking, every retained/public log path passes decode, control filtering, and exact secret redaction, active plus archived admission has finite byte/run/directory ceilings, and terminal cleanup cannot race controls, capture workers, range readers, cancellation, root replacement, or database state changes. External-process contracts still expose no stdout/stderr takeover path. | `cargo fmt --all -- --check`; `cargo check --workspace --all-targets --locked`; targeted P5 crate check; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; staged and unstaged `git diff --check`; strict UTF-8 and trailing-whitespace scan of 254 text files; `rustup target list --installed` | Passed on Windows without warnings. macOS Intel/Apple Silicon compilation and native PTY/root-dirfd retention validation remain explicitly Runner pending. No application, Supervisor, database, migration, fixture, service, test, shell, child process, signal, or real development process was opened or run. |
| 2026-07-16 | P6-T01 | Added a closed HashRouter application shell with a 52 px top bar, 208 px responsive sidebar, five operational routes, default and wildcard redirection to the process workbench, live Supervisor connection presentation, and independent content scrolling. Added validated `system`/`light`/`dark` theme persistence, pre-render document application, native Tauri window-theme synchronization, the minimum `set-theme` capability, and shared StrictMode-safe Supervisor snapshot ownership. The existing 1280 x 800 window and 960 x 640 minimum remain the configured boundary; compact navigation activates below 960 px and secondary top-bar labels collapse below 720 px. Placeholder controls for unimplemented filtering, launch, and detail actions were removed. | Targeted Prettier apply; `pnpm format:check`; `pnpm typecheck`; `pnpm build:web`; `cargo check -p desktop --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; staged and unstaged `git diff --check`; strict UTF-8 and trailing-whitespace scan of 256 text files; independent static P6-T01 review | Windows desktop crate, production web bundle, formatting, frontend types, manifests, routes, theme capability, UTF-8, whitespace, and static StrictMode lifecycle review passed without warnings. macOS Intel/Apple Silicon Tauri compilation remains CI pending. No application, Supervisor, database, migration, fixture, service, test, shell, child process, signal, PTY, ConPTY, or real development process was opened or run. |
| 2026-07-16 | P6-T02 | Added a Supervisor-backed process workbench with a pinned TanStack virtualizer, one bounded scroll viewport, fixed 44 px rows, full process-instance keys, five segmented filters, multi-token search, and deterministic stable sorting across status, name, project, ports, CPU, memory, uptime, and PID. Structured known, unknown, access-limited, unsupported, and missing field states retain distinct text/icon presentation; port cells cap visible summaries and expose complete endpoint text without changing row height. The eight-column model uses responsive secondary-column removal with explicit logical ARIA indices, stable overflow/ellipsis rules, a 36 px sticky-header scroll margin, and bounded roving-focus retention so virtual unmounting cannot discard the active row. Added an explicit production-isolated deterministic 1000-row stress factory covering long Chinese names, fourteen-port rows, managed/external ownership, and unavailable field states. No detail or stop surface was introduced before its planned node. The generated wire contract remains version 12. | Exact catalog install and offline frozen lock verification for `@tanstack/react-virtual` 3.14.6; targeted Prettier apply; `pnpm format:check`; `pnpm typecheck`; `pnpm build:web`; canonical `cargo run -p domain --bin export-types --locked` plus generated-source Prettier after a concurrent stale codegen output was detected; staged and unstaged `git diff --check`; strict UTF-8 and trailing-whitespace scan of 261 text files; independent static P6-T02 review | Production web bundle, formatting, frontend types, virtual coordinate/focus bounds, filter/search/sort projections, responsive ARIA columns, lockfile, version-12 generated contracts, UTF-8, whitespace, stress-data coverage, and production Mock isolation passed without warnings. Runtime scrolling, visual layout, and assistive-technology behavior remain P6-T09 validation because this repository boundary forbids launching the desktop application. No application, Supervisor, database, migration, fixture, service, test, shell, child process, signal, PTY, ConPTY, or real development process was opened or run. |
| 2026-07-16 | P6-T03 | Added a latest-snapshot process inspector selected exclusively by the full boot/PID/native-start identity; disconnect, loss of synchronization, process exit, or identity replacement closes stale details. The desktop workbench opens one 400 px non-modal inspector while viewports below 720 px conditionally render one Radix modal drawer with focus trapping, overlay dismissal, Escape handling, and explicit row/table focus restoration. Radix tabs expose Overview, observed Process Tree, Command, and Classification Reasons without duplicating content. Overview preserves structured field availability, endpoint/state/confidence details, project evidence, and complete instance identity. The tree links only exact parent identities, bounds ancestry at 64 levels, caps rendered child sets, and distinguishes root, unknown, access-limited, unsupported, outside-snapshot, cycle, and depth-limited termination. Command/path/working-directory text is safely wrapped as plain text; environment is separately marked unavailable because the snapshot contract does not expose it. Classification shows category, decision, score, version, override, stable reasons, and bounded project markers. Unknown, restricted, unsupported, missing, and contract-unavailable states use distinct visible text and icons with reasons where present. Table focus and inspector selection are separate, so arrow navigation does not open a drawer. Stop controls remain intentionally absent until P6-T05 instead of exposing dead actions. | Targeted Prettier apply; `pnpm format:check`; repeated `pnpm typecheck`; repeated `pnpm build:web`; staged and unstaged `git diff --check`; strict UTF-8 and trailing-whitespace scan of 265 text files; independent static P6-T03 accessibility, identity, tree-boundary, responsive-dialog, overflow, and capability review | Production web bundle, UI-package primitives, frontend types, full-identity selection, snapshot invalidation, bounded tree projection, structured unavailable states, desktop/mobile dialog semantics, Escape/focus paths, long-text containment, UTF-8, and whitespace passed without warnings. Runtime drawer transitions, actual focus behavior, screen-reader output, and visual overlap remain P6-T09 validation under the repository's no-launch boundary. No application, Supervisor, database, migration, fixture, service, test, shell, child process, signal, PTY, ConPTY, or real development process was opened or run. |
| 2026-07-16 | P6-T04 | Added a Supervisor-backed launch-profile workspace with a bounded paginated list, create/update/delete editor, explicit Direct versus Shell execution, stable argument and environment editing, responsive three-region layout, and a structured final execution preview sourced only from the host execution context. Added typed `profile.list`, `profile.save`, `profile.delete`, and read-only `profile.preview` service/dispatcher boundaries; identity and timestamps remain Supervisor-owned, wire parameters are exact DTOs, and TypeScript validates complete nested responses plus request/result consistency. Generated contracts advanced to version 13 with a typed delete response. Plain secret text remains only in an uncontrolled password input and the single save submission call stack: dirty tracking and preview never construct secret-bearing `FormData`, create/rename/rotation use explicit write-only entries, empty secret rotation has a separate boolean intent, stored credential references are never rendered, and final preview defensively masks inherited, credential-backed, pending, and sensitive-name values. List/preview sequence guards prevent stale responses, clean selected drafts follow refreshed revisions, dirty drafts are retained, and save/delete lock profile switching. Field errors are associated with visible controls for assistive technology. No Run, Start, or non-persistable Detected Script action was exposed. | Stable double version-13 type generation (raw SHA-256 `201D852CA2C2E41AC128A54D32CEBB607E848B7E201B9FB0B991F61C8E454591`) followed by Prettier; `cargo fmt --all -- --check`; `cargo check -p domain -p lifecycle -p protocol -p storage -p supervisor --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; `pnpm build:web`; staged and unstaged `git diff --check`; strict UTF-8, BOM-free, and trailing-whitespace scan of 277 text files; independent RPC and final UI security/concurrency/accessibility reviews | Windows profile service, typed compile-only dispatcher, generated contracts, production web bundle, frontend types, responsive layout constraints, secret write boundary, final-preview validation, refresh/preview race handling, accessibility associations, UTF-8, and whitespace passed without warnings or remaining medium/high review findings. The repository still has no assembled long-running Supervisor host/listener, so runtime routing, credential-store behavior, focus behavior, and visual layout remain later integration/Runner validation; the desktop reports preview unavailable instead of fabricating a local result. macOS Intel/Apple Silicon compilation remains CI pending. No application, Supervisor, database, credential store, migration, fixture, service, test, shell, child process, signal, PTY, ConPTY, or real development process was opened or run. |
| 2026-07-16 | P6-T05 | Added authoritative `process.get_details`, `run.stop`, `run.force_stop`, and `process.stop_external` compile-only dispatch contracts, advanced generated bindings to version 14, and carried `managedRunId` through discovery snapshots under the invariant that managed records have one exact run ID while external records have none. Discovery overlays managed ownership before classification and republishes ownership changes; lifecycle, revision, storage, and Supervisor lookups use the complete validated boot/PID/native-start identity without PID fallback. External stop rechecks managed control immediately before signaling and fails closed on a match or lookup failure. The process inspector now shows direct graceful stop, confirmed exact external stop, and force stop only in the More menu after an authoritative graceful timeout; confirmation is revalidated against the latest snapshot/details before one non-retried mutation. Page-level feedback survives inspector loss, external success states only that the signal was delivered, and dialogs, responsive layout, and reduced motion have explicit behavior. | Stable version-14 generation (raw SHA-256 `96860860361DB99EC9398EA43FA466D615A4BFA94D2A2984165F435CE2F83166`) followed by Prettier; `cargo fmt --all -- --check`; `cargo check -p domain -p lifecycle -p protocol -p storage -p discovery -p platform-windows -p platform-macos -p supervisor --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; repeated `pnpm typecheck`; repeated `pnpm build:web`; staged and unstaged `git diff --check`; strict UTF-8, BOM-free, and trailing-whitespace scan of 282 text files; independent backend and final UI identity/security/concurrency/accessibility reviews | Windows targeted crates, version-14 contracts, production web bundle, frontend types, exact-key consistency, single-flight detail polling, stale-selection invalidation, confirmation binding, stop-result feedback, formatting, UTF-8, and whitespace checks passed with no remaining medium/high review findings. Vite retained its non-failing bundle-size advisory. The repository still has no assembled long-running Supervisor host/listener, so dispatcher runtime routing and live `ManagedRunService` to discovery-binding synchronization remain host-wiring pending and actual snapshots cannot yet be claimed to reflect managed ownership at runtime. macOS Intel/Apple Silicon compilation and native stop behavior remain CI/Runner pending. No application, Supervisor, database, migration, fixture, service, test, shell, child process, signal, PTY, ConPTY, or real development process was opened or run. |
| 2026-07-16 | P6-T06 | Added a strict read-only `run.get_log_range` compile-only dispatcher over the existing validated version-14 DTO and a page-wide collapsible managed-log tool. The UI registers the generation-bound live listener before starting stdout/stderr history reads, isolates every session by Supervisor generation, managed run, and full selected process identity, and keeps one range request in flight per stream. UTF-8 byte-offset fragments merge and deduplicate without JavaScript character-count assumptions; sequence, delivery, or offset gaps reconcile through bounded range reads. Each stream is capped at 512 KiB and 1,024 fragments, with explicit backend rotation/retention, local-memory, and Clear-display omission states. Pause freezes visible snapshots while live events and expanded history reads continue into the bounded model; Clear changes only the local cutoff, and errors require explicit Retry. Collapsing stops history pagination and hidden React snapshot publication while retaining bounded live ingestion. The 220–320 px panel provides independent stdout/stderr views, bounded pure-text search highlighting, pause/resume, local clear, auto-scroll, EOF, encoding/filtering/I/O states, and an explicit external-process no-capture message. | Targeted Prettier apply; `pnpm format:check`; repeated `pnpm typecheck`; repeated `pnpm build:web`; `cargo fmt --all -- --check`; `cargo check -p domain -p lifecycle -p protocol -p storage -p logging -p supervisor --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; staged and unstaged `git diff --check`; strict UTF-8, BOM-free, and trailing-whitespace scan of 286 text files; independent model/controller/resource-boundary and final acceptance reviews | Windows log dispatcher, frontend type model, bounded merge/reconciliation controller, production web bundle, plain-text rendering, collapsed/paused/error concurrency, responsive sizing, formatting, UTF-8, and whitespace checks passed with no remaining medium/high review findings. Generated contracts remain version 14. Vite retained its non-failing bundle-size advisory. The repository still has no assembled long-running Supervisor host/listener, so dispatcher runtime routing, actual `log.chunk` event forwarding, real range reads, scrolling, visual layout, and assistive-technology behavior remain host-wiring/P6-T09/Runner pending. macOS Intel/Apple Silicon compilation remains CI pending. No application, Supervisor, database, migration, fixture, service, test, shell, child process, signal, PTY, ConPTY, or real development process was opened or run. |
| 2026-07-16 | P6-T07 | Added complete Projects and Rules, History, and Settings surfaces backed by strict version-15 DTOs. Project and rule catalogs provide bounded cursor pagination, typed create/update/delete, optimistic version checks, platform-owned path normalization, stable in-process dirty drafts, and deletion that refuses profile or rule references instead of cascading. Successful catalog mutations are idempotent across retries through a 16,384-entry fail-closed SQLite ledger keyed by the authenticated operation ID, method, and canonical typed-request SHA-256; entity CAS/FK/unique checks and the replay DTO are one prepared transaction, published to discovery before commit, with durable-database reconciliation after publish, rollback, or uncertain-commit failures. Catalog versions use `max(now, previous + 1ns)`. Run, stop, recovery, and retention writes now validate complete prospective records before SQL, require strictly advancing canonical UTC nanosecond timestamps, CAS old versions, and never commit an invalid timeline; offline-exit recovery markers remain atomic. History exposes only bounded redacted summaries and omits command, environment, path, secret, exit-summary, and log-directory content. Settings exposes only the implemented system/light/dark theme capability. Loading and mutation sequencing prevents stale reads from overwriting saves or deletes; confirmations stay bound to persisted identities, failures retain dialog context, and focus is explicitly restored. | Stable double version-15 type generation (raw SHA-256 `6A8414598F340643B05B41EFB1FADBCB8738043F37070536E631ED6891457932`) followed by Prettier; `cargo fmt --all -- --check`; `cargo check -p domain -p lifecycle -p protocol -p storage -p discovery -p platform-windows -p platform-macos -p supervisor --all-targets --locked`; `cargo check --workspace --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; repeated `pnpm format:check`; repeated `pnpm typecheck`; repeated `pnpm build:web`; staged and unstaged `git diff --check`; strict UTF-8, BOM-free, and trailing-whitespace scan of 326 text files; independent catalog transaction, frontend concurrency/privacy/accessibility, and storage timeline reviews | Windows workspace, version-15 contracts, migrations `0007` and `0008`, catalog/history dispatchers, production web bundle, frontend types, prepared mutation reconciliation, canonical timeline validation, formatting, UTF-8, and whitespace checks passed without warnings or remaining medium/high review findings. Vite retained its non-failing bundle-size advisory. The repository still has no assembled long-running Supervisor host/listener, so runtime RPC routing, discovery publication, database behavior, focus behavior, and visual layout remain host-wiring/P6-T09/Runner pending. macOS Intel/Apple Silicon compilation remains CI pending. No application, Supervisor, database, migration, fixture, service, test, shell, child process, signal, PTY, ConPTY, or real development process was opened or run. |
| 2026-07-16 | P6-T08 | Added a tray-owned desktop lifecycle with close-to-tray, Show, Open Launch Profiles, Open Settings, and Quit actions, plus system-exit interception and one-use nonce authorization so only a completed explicit decision can terminate the UI. Added strict version-16 contracts for `system.get_exit_impact` and `run.stop_all_for_exit`. The authoritative assessment is a sorted union of in-memory controls and durable active runs, capped at 16 and failed closed above that bound; its SHA-256 binds the Supervisor incarnation to prevent stale-assessment and restart ABA. Migration `0009` adds a bounded 4,096-entry durable StopAll ledger whose first request fixes the member set and request hash. Launching members reserve deterministic child stop operation IDs, Running transitions continue graceful stop, existing graceful or force operations are adopted, and the batch never escalates to force automatically. Later-started work causes Conflict instead of silent inclusion, and Completed is based on a final authoritative snapshot rather than a permanent global launch lock. The frontend decision flow provides Keep tasks running, Stop all tasks, and Cancel; listener-before-snapshot initialization, epochs, nonces, fixed-member comparison, fresh impact reads, and navigation acknowledgement prevent stale asynchronous completion. | Stable double version-16 type generation (raw SHA-256 `374C639548F195DA4DC409EA33CF02FEABC18812E8C1B61ACC9B2C2439B89E6F`) followed by Prettier; `cargo fmt --all -- --check`; `cargo check -p domain -p lifecycle -p protocol -p storage -p supervisor -p desktop --all-targets --locked`; `cargo check --workspace --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; `pnpm build:web`; staged and unstaged `git diff --check`; strict UTF-8, BOM-free, and trailing-whitespace scan of 342 text files; independent backend, native-lifecycle, concurrency, and frontend acceptance reviews | Windows workspace, version-16 generated contracts, migration `0009`, durable fixed-member StopAll semantics, native tray/close/exit compilation, production web bundle, frontend types, formatting, UTF-8, and whitespace passed with no remaining medium/high review findings. Vite retained its non-failing 500 kB bundle advisory. The tray intentionally exposes only real static page shortcuts because no safe idempotent profile-start surface is wired. The repository still has no assembled long-running Supervisor host/listener, so `ExitRpcDispatcher` runtime routing remains pending; tray behavior, Cmd+Q interception, independent Supervisor survival, StopAll signal delivery, and visual/focus behavior were not run. macOS Intel/Apple Silicon compilation remains CI/Runner pending. No application, Supervisor, database, migration, fixture, service, test, shell, child process, signal, PTY, ConPTY, or real development process was opened or run. |
| 2026-07-16 | P6-T09 | Added a user-selectable 25-row Paged process-table mode alongside the default virtual mode so assistive technology can traverse a complete bounded page while retaining exact logical row/column indices, deterministic filter/search/sort projection, selection, and cross-page Arrow/Home/End keyboard focus. Route changes now announce the page and move focus to its main landmark after the initial route. Launch-profile saves, deletes, argument removals, and environment removals restore focus to a stable heading, Name field, adjacent input, or Add control after remounts. Exit-phase text and log diagnostics have bounded live announcements; paused high-volume logs expose one static announcement while visual counters remain outside the accessibility tree. Light muted text was raised to a 4.60:1 minimum on the elevated surface, form controls use a separate 3:1 boundary token, and `prefers-contrast` plus forced-colors modes preserve selected, focused, status-icon, and disabled distinctions. Responsive rules close the 704-719 px secondary-column gap, make the narrow log toolbar wrap predictably, keep pagination bounded, and shrink the expanded log panel on short viewports without viewport-scaled font sizes. | Targeted Prettier apply; repeated `pnpm format:check`; repeated `pnpm typecheck`; repeated `pnpm build:web`; staged and unstaged `git diff --check`; strict UTF-8, BOM-free, and trailing-whitespace scan of 342 text files; independent keyboard/ARIA/concurrency and contrast/forced-colors/320-960 px responsive reviews | Frontend formatting, types, production bundle, semantic paging, route and mutation focus restoration, bounded live regions, WCAG text/control contrast, forced-colors selected-focus distinction, reduced-motion preservation, short-height behavior, 320/480/720/960 px static constraints, UTF-8, and whitespace passed with no remaining medium/high review findings. Vite retained its non-failing 500 kB bundle advisory. Runtime focus order, actual system 100/125/150/200% scaling, Windows high-contrast themes, Narrator, VoiceOver, virtual-table reading, visual overlap, and native window behavior remain explicit assistive-technology/Runner validation because the desktop application was not started. No application, Supervisor, database, migration, fixture, service, test, shell, child process, signal, PTY, ConPTY, or real development process was opened or run. |
| 2026-07-16 | P6 gate | Rechecked the complete operational shell, process/detail/stop/log workflows, launch profiles, project/rule catalogs, history, settings, tray exit decisions, keyboard paths, responsive layouts, and state presentations. First-use/empty, snapshot loading, Supervisor unavailable/reconnecting/access-denied/update-required/shutting-down, identity change, failed history, stop progress/timeout/force confirmation, log omission/overload/encoding, and generic startup failure remain visibly distinct through text and icons rather than color alone. Layout uses bounded work areas, one level of operational panels, fixed table rows, responsive column removal, scroll containment, reduced motion, and no marketing hero, card nesting, neon background, or viewport-scaled type. | `cargo fmt --all -- --check`; `cargo check --workspace --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; `pnpm build:web`; staged and unstaged `git diff --check`; strict UTF-8/BOM/whitespace scan; `rustup target list --installed`; independent final P6-T09 reviews | Windows workspace and the complete production web UI compiled without warnings or remaining medium/high static review findings; only the existing non-failing Vite bundle-size advisory remains. This host has only `x86_64-pc-windows-msvc`, so macOS Intel/Apple Silicon compilation remains CI pending. Real tray, lifecycle, focus, assistive-technology, scaling, contrast-theme, migration-failure producer, update-waiting producer, and visual behavior remain Runner or later-node integration boundaries and are not claimed as tested. No application, Supervisor, database, migration, fixture, service, test, shell, child process, signal, PTY, ConPTY, or real development process was opened or run. |
| 2026-07-16 | P7-T01 | Added bounded batch enrichment for complete visible and selected process identities, one in-flight/latest-pending desktop scheduling, 100 ms visible debounce, immediate selected priority, and a 10-second unchanged-identity refresh. Discovery now records bounded completed/failed/cancelled counts and 256-sample p50/p95 windows for fast, port, enrichment, and project work; accepted enrichment remains successful when independent project queuing fails. A 16 ms last-write-wins event accumulator caps each mutation event at 128 entities and 512 KiB, rejects an oversized single entity before publication, and reports process publication separately from its possible scan/enrichment/project origins. Revision state now rejects unpageable deltas before mutation, caps process/port state at 16,384/65,536 entities, 512 KiB per conservatively accounted entity, and 128 MiB total, then serves a random-ID frozen revision through ordered 1,024-entity/768 KiB chunks. Four sessions, 256 MiB pinned data, 30-second idle and five-minute absolute expiry bound retained generations. The desktop strictly validates version-18 DTOs, chunk continuity, fixed totals, duplicate/cross keys, delta size, projected collection bytes, and an 8 MiB event buffer before atomic commit; malformed data is fail-closed with at most one Bridge connection reset and bounded reset-command retry. Added the reproducible reference-host and pending-measurement contract without fabricating results. | Stable double version-18 type generation (raw tree SHA-256 `85A09F2ED6256B6C33CCC3737C07E89741B285FF89DEFD0642A209AF2BB423C2`) followed by Prettier; `cargo fmt --all -- --check`; `cargo check -p domain -p discovery -p protocol -p supervisor -p desktop --all-targets --locked`; `cargo check --workspace --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; `pnpm build:web`; staged and unstaged `git diff --check`; strict UTF-8, BOM-free, and trailing-whitespace scan of 347 text files; repeated independent backend, discovery, frontend concurrency, DTO, resource, cursor, and recovery reviews | Compiled on Windows with no remaining medium/high static review findings. The production bundle retained only the existing non-failing 500 kB advisory. Real p95 values, new-process visibility latency, macOS Intel/Apple Silicon compilation, native macOS targeted-port expiry behavior, and real load remain Runner pending. The repository still has no assembled long-running Supervisor host/listener or discovery-to-revision/total-dispatcher route, so enrichment execution, revision event delivery, snapshot RPC delivery, Bridge reset recovery, and collected performance metrics are not claimed as runtime-tested. No application, Supervisor, database, migration, fixture, service, test, shell, child process, socket, native scan, signal, PTY, ConPTY, or real development process was opened or run. |
| 2026-07-16 | P7-T02 | Added version-19 diagnostic contracts, a strict manifest read and mutating export dispatcher, and a Supervisor diagnostics service over a bounded structured application-log ring. Public events accept only fixed fields, JSON-safe numbers, and trusted static identifiers; free-form messages, commands, paths, environment values, RPC parameters, and managed stdout/stderr cannot be represented. The default manifest includes only build/platform/protocol metadata; users must explicitly include the 4 MiB/4,096-record redacted application-log tail or aggregate-only database counts. One UTF-8 JSON package is capped at 64 MiB. A fixed private export root uses one create-new partial, required-source identity-checked atomic replacement, file/directory sync, cancellation cleanup, crash recovery, and eight rotating slots, bounding retained service output to 512 MiB. Pending/caller/worker leases keep one export active until detached blocking work actually ends; slot overwrite invalidates stale in-memory replay. Settings shows the server manifest before export, defaults optional content off, suppresses stale generations, validates exact responses, and returns only a safe slot file name, byte count, and SHA-256. | Stable double version-19 generation (raw tree SHA-256 `CBCB9BCE507C8225E6FB2DBDE75071F4B765A8DBCFA7A97EA1124C899EB16841`) followed by Prettier; `cargo fmt --all -- --check`; `cargo check -p domain -p lifecycle -p logging -p storage -p protocol -p supervisor -p desktop --all-targets --locked`; `cargo check --workspace --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; `pnpm build:web`; staged and unstaged `git diff --check`; strict UTF-8, BOM-free, and trailing-whitespace scan of 362 text files; repeated independent privacy, cancellation, atomic-publication, replay, DTO, storage, and frontend reviews | Windows workspace, generated contracts, structured redaction boundary, aggregate database projection, bounded private export, frontend manifest flow, formatting, types, metadata, UTF-8, and whitespace compiled without warnings or remaining medium/high static review findings. Vite retained only the existing non-failing 500 kB advisory. macOS Intel/Apple Silicon compilation, native ACL/rename behavior, actual JSON publication, cancellation under real IO, and UI/focus behavior remain Runner pending. The operation replay ledger is intentionally in-memory and slot-bounded; Supervisor restart may rebuild the same operation into a slot rather than claim durable export replay. The repository still has no assembled long-running Supervisor host/listener or total dispatcher, so diagnostic events, RPC routing, database aggregates, and export creation are not claimed as runtime-tested. No application, Supervisor, database, migration, fixture, service, test, shell, child process, socket, native scan, signal, PTY, ConPTY, or real development process was opened or run. |
| 2026-07-16 | P7-T03 | Reduced the main Tauri window to event listen/unlisten and theme permissions, separated localhost development CSP from the self/IPC-only production CSP, and added pre-queue Bridge limits of 768 KiB encoded JSON, 32 levels, 8,192 nodes, 1,024 object keys, 4,096 array entries, 64 KiB strings, and 256-byte keys. Replaced arbitrary SQLite paths with a non-Serde `PrivateDatabasePath` capability for the fixed current-user data root and `supervisor.sqlite3`. Windows rejects reparse points, non-disk objects, non-current owners, hard links, and non-protected/non-current-SID DACLs for the app/data roots and DB/WAL/SHM; macOS walks from the passwd home with `O_NOFOLLOW`, verifies effective UID and link count, removes allow ACLs, and enforces 0700/0600. Repository opening revalidates before connection, after connection, and after migrations. Migration `0010` extends the unchanged `0008` mutation ledger to `profile.save/delete`, preserves prior rows, and caps result JSON at 256 KiB. Profile mutations replay before allocating IDs, timestamps, credential references, or OS credential writes; the profile/environment write, credential adoption/cleanup queue, and compact replay result share one transaction. The secret-bearing request is bound with streaming HMAC-SHA256 under a random 256-bit key held only in the system credential store through a purpose-separated internal reference; missing key plus existing save ledger fails closed. Compact replay persists no secret, command, path, or plain environment. Profile versions remain strictly monotonic across clock rollback. The frontend retains only a request SHA-256 and operation ID so an exact failed save/delete intent reuses its durable operation while changed content receives a new ID. | `cargo fmt --all -- --check`; `cargo check -p platform-common -p protocol -p lifecycle -p storage -p supervisor -p desktop --all-targets --locked`; `cargo check --workspace --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; `pnpm build:web`; staged and unstaged `git diff --check`; strict UTF-8, BOM-free, and trailing-whitespace scan of 366 text files; independent Tauri/IPC/database-path and repeated profile idempotency/privacy reviews | Windows workspace, fixed private database capability, platform permission code, Bridge limits, production CSP, minimal capability, HMAC-bound durable profile replay, migration `0010`, strictly monotonic versions, frontend retry intent, formatting, types, metadata, UTF-8, and whitespace compiled without warnings or remaining medium/high static review findings. Vite retained only the existing non-failing 500 kB advisory. macOS Intel/Apple Silicon compilation and native ACL/sidecar behavior remain CI/Runner pending. The repository still has no assembled long-running Supervisor host/listener or total dispatcher, so runtime RPC routing and authenticated request dispatch are not claimed as exercised. No application, Supervisor, database, migration, credential store, fixture, service, test, shell, child process, socket, native scan, signal, PTY, ConPTY, or real development process was opened or run. |
| 2026-07-17 | P7-T04 | Added a repository-lifetime in-process guard and fixed private cross-process `migration.lock`, then moved all SQLite inspection, backup, migration, and recovery behind that exclusive boundary. Existing databases accept only a clean strict prefix of the embedded transactional migration manifest; dirty, unknown, skipped, reordered, checksum-mismatched, oversized, or `no_tx` migrations fail closed. A structurally empty legacy database is treated as an empty existing source and is still backed up. Pending migrations alone create a bounded consistent `VACUUM INTO` snapshot, validate its manifest, health, size, identity, and SHA-256, rotate two fixed slots, and atomically publish a 128 KiB marker containing the complete source and target fingerprints. Startup handles the marker before any ordinary SQLite open, validates its referenced backup even when the active database appears complete, restores through an identity-checked fixed partial, and never exposes a Repository during a recovery or migration failure. Fresh-database failures durably discard only newly created SQLite artifacts. Main, WAL, SHM, rollback journal, backup partial journal, restore partial, marker, backup slots, logical database size, and retained data all have fixed names and explicit bounds; missing Main plus any data-bearing sidecar or recovery artifact fails closed. SQLite artifacts are truncated and synced before deletion to prevent stale WAL replay after power loss. Windows requires a fixed local drive and matching handle volume serial, rejects reparse points/hard links/unsafe ownership or DACLs, and uses write-through replacement; macOS requires `MNT_LOCAL`, one filesystem ID, no-follow opens, current UID, 0700/0600, no allow ACLs, one link, and synced directory operations. The final pool uses `create_if_missing(false)` and is returned only after complete path and artifact revalidation. | `cargo fmt --all -- --check`; `cargo check -p storage -p supervisor --all-targets --locked`; `cargo check --workspace --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; staged and unstaged `git diff --check`; strict UTF-8, BOM-free, and trailing-whitespace checks for the nine involved files; repeated independent SQLx, crash-window, filesystem, resource-limit, and recovery-state reviews | Windows workspace compiled without warnings or remaining medium/high static review findings. macOS Intel/Apple Silicon compilation and native `fstatfs`, APFS rename/fsync, and ACL behavior remain CI/Runner pending. Real N-1 to N migration, backup/restore, crash injection, disk-full, Windows power-loss, and concurrent-process behavior were not executed. No application, Supervisor, database, migration, fixture, test, service, socket, child process, or real development process was opened or run. |
| 2026-07-17 | P7-T05 | Replaced the empty fixture crate with a default-disabled `fixture-runtime` feature and one `required-features` binary while keeping the ordinary library free of any public run or start API. Every external invocation must supply the fixed CLI authorization flag and exact acknowledgement environment value before a watchdog thread, process, socket, or payload write is created. Internal child and grandchild roles require the same public acknowledgement plus a fresh 256-bit session nonce in an otherwise cleared environment. The fixed process-tree fixture launches only its own executable, caps the tree at root/child/grandchild, supports stable and child-exits-after-spawn cases, bounds readiness and cleanup waits, reaps the fast child before announcing readiness, requires successful natural exits, and gives every role a non-blocking 60-second hard deadline. The network fixture binds one TCP listener and one UDP socket only to `127.0.0.1`, `0.0.0.0`, `::1`, or `::`, always requests port zero, and never accepts, connects, or sends. The log fixture caps each stream at 32 MiB and the aggregate at 64 MiB, shares an aggregate 8 MiB/s rate, limits lines to 256 KiB and writes to 64 KiB, uses fixed ANSI/OSC content, and treats BrokenPipe as bounded stream completion. Seven fixed encoding scenarios cover split and truncated UTF-8, invalid bytes, split UTF-16LE/BE BOMs, Windows-1252, and ANSI/OSC; readiness stays on stderr so stdout detection remains uncontaminated and payloads remain below 64 KiB. Platform gates are exact for Windows x64 and macOS x86_64/aarch64; unsupported feature builds fail. | `cargo fmt --all -- --check`; `cargo check -p test-fixtures --all-targets --locked`; `cargo check -p test-fixtures --all-targets --features fixture-runtime --locked`; `cargo check --workspace --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; staged and unstaged `git diff --check`; strict UTF-8, BOM-free, and trailing-whitespace checks for the 12 involved files; independent plan, compilation, authorization, resource, deadline, cleanup, network, log, and encoding reviews | Windows x64 default, test-target, explicit-runtime, and full-workspace compilation passed without warnings or remaining medium/high static review findings. macOS Intel/Apple Silicon compilation and all real fixture behavior remain CI/Runner pending. No test, fixture binary, child process, socket, application, Supervisor, database, migration, service, or real development process was opened or run. |
| 2026-07-17 | P7-T06 | Added a separate read-only supply-chain workflow with only weekly schedule and manual triggers, non-cancelling concurrency, 60-minute jobs, non-persisted checkout credentials, exact Node/pnpm/Rust/cargo-audit/cargo-deny/cargo-cyclonedx/cdxgen versions, and every GitHub Action pinned by full commit SHA. The ordinary three-target compile workflow no longer uses a movable pnpm Action or dependency cache and now disables npm audit/funding calls and uses locked Cargo resolution. Added a three-target `deny.toml` that rejects wildcard, unknown-registry, and Git dependencies under a closed SPDX allowlist with exact reviewed MPL exceptions. JavaScript licensing consumes bounded structured pnpm JSON, rejects malformed/unknown/duplicate packages, uses exact expressions and package-version exceptions, and separately forbids all exceptions in production dependencies. CycloneDX 1.5 generation covers the complete pnpm workspace and every Rust workspace package with all features for Windows x64 and both macOS targets, validates every JSON document, detects lockfile mutation, and retains one private CI artifact for 14 days. pnpm 11 peer installation is now explicitly disabled in workspace configuration and the lockfile; the UI package declares its React peers as exact development dependencies so clean isolated builds remain resolvable. Added the verification boundary and review checklist without claiming an audit or SBOM was executed. | `pnpm install --lockfile-only --ignore-scripts --force --config.auto-install-peers=false`; `pnpm install --frozen-lockfile --ignore-scripts`; `node --check scripts/check-js-licenses.mjs`; full and production `pnpm licenses list --json` policy checks plus unknown/missing/production-MPL negative cases; JSON and TOML parsing; workflow trigger/SHA/version/timeout/artifact/credential assertions; cargo-cyclonedx 0.5.7 source-level CLI/output review; `pnpm format:check`; `pnpm typecheck`; repeated `pnpm build:web`; `cargo fmt --all -- --check`; `cargo check --workspace --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; staged and unstaged `git diff --check`; strict UTF-8, BOM-free, and trailing-whitespace checks | Windows frontend and full Rust workspace compilation passed. The first web build after disabling auto-installed peers correctly exposed an undeclared UI JSX runtime; adding exact React/ReactDOM development dependencies and regenerating the lockfile fixed it, after which frozen install, formatting, types, production build, licenses, metadata, and workspace compilation passed. Vite retained only the existing non-failing 500 kB advisory. `cargo-audit`, `cargo-deny`, `cargo-cyclonedx`, cdxgen, and `pnpm audit` were not installed or run; the scheduled/manual workflow and macOS target jobs have not run, and no external audit service was contacted. No application, Supervisor, test, fixture, database, migration, socket, child process, or real development process was opened or run. |
| 2026-07-17 | P7 gate | Rechecked the bounded discovery scheduler and event accumulator, managed and application log retention, crash-safe migration artifacts, authenticated IPC framing and Bridge queues, minimal Tauri capability and production CSP, redacted diagnostic export, closed license policies, and scheduled/manual-only audit and SBOM configuration. | `pnpm install --frozen-lockfile --ignore-scripts`; `pnpm format:check`; `pnpm typecheck`; `pnpm build:web`; `pnpm run licenses:check:js`; `cargo fmt --all -- --check`; `cargo check --workspace --all-targets --locked`; `cargo metadata --format-version 1 --no-deps --locked`; JSON/TOML/workflow policy parsing and assertions; staged and unstaged `git diff --check`; strict UTF-8/BOM/whitespace checks; `rustup target list --installed`; `git remote -v`; `git rev-parse --verify HEAD`; independent final gate review | All P7 static gates and the Windows x64 compile passed with no remaining static blocker. The existing non-failing Vite 500 kB advisory remains. The three-target gate is not complete: this host has only `x86_64-pc-windows-msvc`, the repository has neither a Git `HEAD` nor a remote, and the configured macOS Intel/Apple Silicon jobs have never run. P8 was not started because the plan forbids crossing an unmet phase gate. No application, Supervisor, audit service, test, fixture, database, migration, socket, child process, signing, installation, update, or real development process was opened or run. |
| 2026-07-17 | P7 gate follow-up | Revalidated the configured three-target compile path against GitHub's current official hosted-runner table. Replaced the retired `macos-13` Intel label with the supported `macos-15-intel` label, retained the native arm64 `macos-14` job, added explicit x64/arm64 Node host assertions so a label drift cannot silently cross-compile on the wrong architecture, and added a separate compile-only check for the default-disabled authorized fixture feature on every supported target. | GitHub-hosted runners official reference inspection; `pnpm exec prettier --check .github/workflows/compile.yml`; compile-matrix policy assertions; local Node x64 architecture assertion; `cargo check --workspace --all-targets --locked --target x86_64-pc-windows-msvc`; `cargo check -p test-fixtures --all-targets --features fixture-runtime --locked --target x86_64-pc-windows-msvc`; `cargo fmt --all -- --check`; `cargo metadata --format-version 1 --no-deps --locked`; `pnpm format:check`; `pnpm typecheck`; `pnpm build:web`; staged and unstaged `git diff --check` | The corrected workflow is statically valid and the exact Windows x64 equivalents compile. Vite retained only the existing non-failing 500 kB advisory. The P7 gate remains blocked solely on execution evidence: this repository still has no Git `HEAD` or remote, so the corrected macOS Intel and Apple Silicon jobs have not run. A configured current Runner label is not recorded as a compiled target. P8 remains unstarted under the no-gate-skipping rule. No application, Supervisor, test, fixture, database, migration, socket, child process, signing, installation, update, or real development process was opened or run. |
