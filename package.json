{
  "name": "dev-process-manager-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first development process manager",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build:web": "pnpm --filter @dpm/desktop run build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "generate:types": "cargo run -p domain --bin export-types && prettier --write packages/generated-types/src",
    "licenses:check:js": "pnpm licenses list --json | node scripts/check-js-licenses.mjs && pnpm licenses list --prod --json | node scripts/check-js-licenses.mjs --production",
    "typecheck": "pnpm -r --if-present run typecheck"
  },
  "devDependencies": {
    "prettier": "3.6.2",
    "typescript": "7.0.2"
  }
}
