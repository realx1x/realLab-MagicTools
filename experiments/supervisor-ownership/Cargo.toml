[package]
name = "supervisor-ownership-spike"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
thiserror = "=2.0.17"
tokio = { version = "=1.47.1", features = [
    "fs",
    "io-util",
    "process",
    "rt",
    "sync",
    "time",
] }

[workspace]
