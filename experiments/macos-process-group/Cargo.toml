[package]
name = "macos-process-group-spike"
version = "0.1.0"
edition = "2024"
publish = false

[target.'cfg(target_os = "macos")'.dependencies]
libc = "=0.2.186"
nix = { version = "=0.31.3", default-features = false, features = ["process", "signal"] }

[workspace]
